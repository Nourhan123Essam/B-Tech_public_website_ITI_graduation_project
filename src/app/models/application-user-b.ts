export interface ApplicationUserB {

        id: string;  // IdentityUser ID
        address: string;
        city: string;
        postalCode: string;
        country: string;
        userType: string;
    
      
}

